module.exports.info = {
    name: 'verification', // default = file name (without the extention)
    description: "Setup the verification system.", // default is "None"
    requiredPermission: "ADMINISTRATOR", // default is null
    aliases: ['verify'], // default is null
}